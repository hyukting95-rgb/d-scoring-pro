# 进入项目目录
cd "D:\d-scoring-pro-management-system"

# 设置Git身份
git config user.name "hyukting95"
git config user.email "hyukting95@gmail.com"

# 添加所有文件
git add .

# 提交代码
git commit -m "Initial commit: D-Scoring Pro system"

# 推送代码到GitHub
git push origin --all

Write-Host "Git身份设置完成，代码推送中..."
